from __future__ import annotations

from .models import InvestorInput, RankResult


def rank_regions(investor: InvestorInput) -> list[RankResult]:
    """Ранжирование ТОП-3 регионов (пока заглушка/каркас).

    Далее сюда подключается:
    - загрузка JSON базы регионов
    - расчет логистических расстояний (сталь/утеплитель)
    - учет льгот, тарифов, газа/мощности
    - весовая модель
    """

    # В каркасе возвращаем фиксированные записи, чтобы UI/API работали end-to-end.
    base = [
        RankResult("mosreg", "Московская область", 0.78, ["Развитая инфраструктура", "Близость рынков сбыта"]),
        RankResult("tatarstan", "Республика Татарстан", 0.74, ["Промышленный потенциал", "Льготы ОЭЗ/ТОР"]),
        RankResult("lenobl", "Ленинградская область", 0.69, ["Портовая логистика", "Кадровый рынок"]),
    ]

    return base[:3]
